{
  "name": "awesome-hosts-manager",
  "version": "1.0.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "preact": "^10.26.9"
  },
  "scripts": {
    "dev": "esbuild src/index.tsx --bundle --format=esm --outfile=public/dist/bundle.js --watch",
    "build": "esbuild src/index.tsx --bundle --outfile=public/dist/bundle.js --minify --sourcemap"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "typescript": "^5.8.3"
  }
}
